{
  "name": "autotel-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Write Once, Observe Anywhere - Zero-boilerplate OpenTelemetry for JavaScript/TypeScript",
  "workspaces": [
    "packages/*",
    "apps/*",
    "examples/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "turbo format",
    "test": "turbo test",
    "test:integration": "turbo test:integration",
    "test:playwright-e2e": "pnpm --filter @jagreehal/example-playwright-e2e test",
    "clean": "turbo clean",
    "type-check": "turbo type-check",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish",
    "quality": "turbo run build && turbo run lint && turbo run format && turbo run type-check && turbo run test && turbo run test:integration",
    "intent:validate": "intent validate packages/autotel/skills && intent validate packages/autotel-hono/skills && intent validate packages/autotel-tanstack/skills && intent validate packages/autotel-cloudflare/skills && intent validate packages/autotel-subscribers/skills && intent validate packages/autotel-edge/skills && intent validate packages/autotel-mcp/skills"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "@eslint/js": "^10.0.1",
    "@tanstack/intent": "^0.0.19",
    "@typescript-eslint/eslint-plugin": "^8.57.0",
    "@typescript-eslint/parser": "^8.57.0",
    "esbuild": "^0.27.4",
    "eslint": "^10.0.3",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-unicorn": "^63.0.0",
    "prettier": "^3.8.1",
    "turbo": "^2.8.17",
    "typescript-eslint": "^8.57.0"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=8"
  },
  "packageManager": "pnpm@10.27.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jagreehal/autotel.git"
  },
  "author": "Jag Reehal <jag@jagreehal.com> (https://jagreehal.com)",
  "license": "MIT",
  "keywords": [],
  "bugs": {
    "url": "https://github.com/jagreehal/autotel/issues"
  },
  "homepage": "https://github.com/jagreehal/autotel#readme",
  "overrides": {
    "vite": ">=7.1.11"
  }
}
