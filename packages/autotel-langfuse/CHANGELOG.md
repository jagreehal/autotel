# autotel-langfuse

Nothing has been released yet. This file is where `changesets` writes the
release notes on the first publish; the pending entry lives in
`.changeset/langfuse-compatibility.md` until then.
