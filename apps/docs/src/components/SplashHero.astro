---
import Hero from './landing/Hero.astro';
---

{/* Theme is resolved before paint by ThemeProvider (defaults to dark on first
    visit, then follows the stored preference). The landing renders in both. */}
<Hero />
