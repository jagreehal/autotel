---
'autotel-terminal': minor
'autotel-plugins': minor
---

- **autotel-plugins**: Kafka plugin EDA enhancements — add `withProducerSpan` and `injectTraceHeaders` for PRODUCER semantics, processing-span context mode, batch lineage attributes, and correlation ID header support.
- **autotel-terminal**: Terminal trace viewer updates — README and setup docs, internal refactor (lib/), and CHANGELOG.
