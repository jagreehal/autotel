---
'autotel-devtools': patch
---

GenAI tab: `execute_tool` spans are titled by tool name (`tool: cities`) in the list row and detail header, with a wrench icon, and the tool name is searchable in the filter box.
